#pragma once
#include <vector>
#include "../Entities/Entity.h"
//#include "Regiment.h"


class Forces{
    private:
	// std::string name;
	// Region * region;
	// vector<Regiment *> * airForce;
	// vector<Regiment *> * army;
	// vector<Regiment *> * navy;
	// void attack(vector<Regiment *> * current,
		// vector<Regiment *> * opposing);
	// void shuffle(vector<Regiment *> * current,
		// vector<Regiment *> * opposing);
    // public:
	// Forces(std::string name);
	// Forces(std::string name, vector<Regiment *> * airForce,
		// vector<Regiment *> * army,vector<Regiment *> * navy);
	// ~Forces();
	// void addToArmy(Regiment * regiment);
	// void addToNavy(Regiment * regiment);
	// void addToAirForce(Regiment * regiment);
	// std::string battleTurn(Forces * opposing);
	// void update();
};
