#pragma once
#include "Regiment.h"


class Forces{
    private:
	std::string name;
	Region * region;
	vector<Regiment *> * airForce;
	vector<Regiment *> * army;
	vector<Regiment *> * navy;
    public:

};